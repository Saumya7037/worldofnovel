# ⚠️  IMPORTANT: This file is a TEMPLATE. Do NOT commit real secrets to GitHub!
# This is already blocked by .gitignore
# Fill in the actual Base64-encoded values before applying.
#
# To encode a value: Run this in your terminal:
# [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes("your-value-here"))
#
# Example for DATABASE_URL:
# [Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes("postgresql://novel_admin:YOURPASSWORD@psql-worldofnovel-saumyak-2026.postgres.database.azure.com:5432/worldofnovel_db?sslmode=require"))

apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: default
type: Opaque
data:
  # Replace the values below with your real Base64-encoded strings
  DATABASE_URL: REPLACE_WITH_BASE64_ENCODED_DATABASE_URL
  JWT_SECRET: REPLACE_WITH_BASE64_ENCODED_JWT_SECRET
